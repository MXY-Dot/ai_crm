# ????????? ?????????? Website, Telegram, Chatwoot ? Dify

? ??????? ????????? ?????????? ???????? ???????? ??? ??????? workspace. ???? ? ??? ??? ???????????? ? ?????? workspace, ?????? ???????? ??????????? ???? Chatwoot ? Dify ??????????: ???? ?????, ???? webhook secret, ???? `tenant_slug`.

## 1. ????????? workspace ????????????

? CRM ??????? ??? ?????? ????????????? ? ???????? ????????? ??????????. ??? ??????????? ??? ????????? ?????? ? ???????? workspace.

Webhook URL ?????? ?????? ????????? slug workspace:

```text
https://YOUR_DOMAIN/api/chatwoot/webhook?tenant_slug=demo
https://YOUR_DOMAIN/api/chatwoot/webhook?tenant_slug=test
```

??? Telegram ???????????? ????????? endpoint:

```text
https://YOUR_DOMAIN/api/telegram/webhook?tenant_slug=demo
```

## 2. Chatwoot

? CRM ???????? `Settings -> Integrations -> Chatwoot` ? ?????????:

- `URL`: ??????? ????? Chatwoot, ???????? `https://chatwoot.example.com`
- `Account ID`: id ???????? ? Chatwoot
- `API Token`: ????? ???????????? ??? ???? Chatwoot
- `Webhook Secret`: ????? ????????? ??????, ?????????? ? CRM ? Chatwoot

? Chatwoot ???????? webhook:

```text
POST https://YOUR_DOMAIN/api/chatwoot/webhook?tenant_slug=YOUR_WORKSPACE_SLUG
```

???? ???????? `Webhook Secret`, Chatwoot ?????? ?????????? ??? ? ?????????:

```text
X-Webhook-Secret: YOUR_SECRET
```

??????? `Test` ? CRM. ???? ?????? `Chatwoot returned HTTP 404`, ?????? ???????? `URL` ??? `Account ID`.

## 3. Dify

? CRM ???????? `Settings -> Integrations -> Dify` ? ?????????:

- `URL`: ???????? `https://api.dify.ai/v1`
- `API Key`: ???? ?????????? Dify
- `Timeout`: ????? ???????? 12 ??????
- `Handoff threshold`: ??????? ???????????, ???? ???????? ?????? ?????? ?????????

??????? `Test`. ? ??????? workspace ????? ???? ???? Dify app ? ???? ????.

## 4. Telegram ????? ngrok

? `.env` ?????? ????:

```env
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_WEBHOOK_SECRET=your_secret
```

????? ????????? `.env` ?????????:

```powershell
php artisan optimize:clear
```

????????? ngrok ?? ???? CRM:

```powershell
ngrok http 8000
```

????????? webhook ? Telegram. URL ?????? ????????? `url` ?????? ???? encoded:

```powershell
curl.exe "https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook?url=https%3A%2F%2FYOUR_NGROK_DOMAIN%2Fapi%2Ftelegram%2Fwebhook%3Ftenant_slug%3DYOUR_WORKSPACE_SLUG&secret_token=YOUR_SECRET"
```

????????:

- ????????? ????????? ???? ? Telegram;
- ? ngrok ?????? ????????? `POST /api/telegram/webhook` ?? ???????? `201`;
- ? CRM ???????? `Inbox`, ?????? ????????? Telegram-??????;
- ????? ?? CRM ?????? ???? ??????? ???????????? ? Telegram.

## 5. Website

? ??????? MVP website-????? ????????? ????????? ????? ????? inbound webhook:

```text
POST https://YOUR_DOMAIN/api/chatwoot/webhook?tenant_slug=YOUR_WORKSPACE_SLUG
```

??????????? JSON:

```json
{
  "provider": "website",
  "conversation": {
    "id": "site-123",
    "subject": "?????? ? ?????"
  },
  "sender": {
    "name": "Client",
    "email": "client@example.com"
  },
  "message": {
    "id": "msg-123",
    "content": "????????????, ???? ????????????"
  }
}
```

???? ? workspace ???????? `Webhook Secret`, ???????? ?????????:

```text
X-Webhook-Secret: YOUR_SECRET
```

## 6. ??????? ???????? ?? PowerShell

```powershell
curl.exe -X POST "https://YOUR_DOMAIN/api/chatwoot/webhook?tenant_slug=YOUR_WORKSPACE_SLUG" -H "Content-Type: application/json" -H "X-Webhook-Secret: YOUR_SECRET" -d "{\"provider\":\"website\",\"conversation\":{\"id\":\"site-test-1\",\"subject\":\"???? ?????\"},\"sender\":{\"name\":\"Test Client\",\"email\":\"test@example.com\"},\"message\":{\"id\":\"site-msg-1\",\"content\":\"???????? website\"}}"
```

????????? ?????????: HTTP `201`, ????? ????? ????????? ????? ? `Inbox`.